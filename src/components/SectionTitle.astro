---
const { title, subtitle } = Astro.props;
---
<header class="mx-auto mb-10 max-w-2xl text-center">
  <h2 class="text-3xl font-semibold md:text-4xl">{title}</h2>
  {subtitle && <p class="mt-3 text-slate-600">{subtitle}</p>}
</header>
